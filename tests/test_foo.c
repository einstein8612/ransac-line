#include "ransac_line.h"   // works now because include/ is in the path

#include <assert.h>
#include <stdio.h>

int main(void) {
    assert(foo() == 10);
    printf("Test passed\n");
    return 0;
}